# Object 272 ProjectChange research

Authoritative corpus: Садовническая 76 / Балчуг Эстейт, OLD=stage_1,
NEW=stage_2, logical v002 (including its explicitly unchanged v001 members).
Artifacts: `/home/coder/auditmanager/corpus-audits/20260914_project_change_272/`.

The immutable `INVENTORY.json`, `OVERLAP_AUDIT.json`, `SPLIT.json` and
`SPLIT.sha256` precede algorithm changes. There are 21 in-scope full pairs:
13 DEV, 4 VALIDATION, 4 FINAL_HOLDOUT. The original 23-pair catalog also contains
one confirmed ALIA pair and a page extract of electrical pair 10. Neither is
an independent eligible pair. Six unmatched documents remain quarantined.

All pairs are historically DEV-known. Neither reserve is historically blind.
The split establishes prospective cipher isolation, with exact shared evidence
pages embargoed in every partition. Paraphrases, image duplicates, shared subjects
and historical architecture exposure remain residual leakage risks. No metric may
be advertised as a clean historical blind estimate or cross-project validation.

Use `policy.admitted_pairs()` for source admission. It verifies the split and
the v002 source hashes, stage direction and physical version; reserve access
requires the corresponding candidate freeze. Keep tuning on DEV. Predictions,
source audit and coverage inventories remain separate artifacts. Never count
unadjudicated output as correct or use zero output as precision evidence.

```sh
python -m unittest experiments.project_change_272.test_policy
```

The archived `project_change_master` cross-project selectors and DATA_BLOCKED
conclusion are superseded. Algorithms remain offline while this program evaluates
EvidenceScope -> EngineeringSubject -> OLD/NEW state -> ProjectChange.
