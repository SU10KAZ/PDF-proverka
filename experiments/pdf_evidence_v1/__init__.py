"""PDF Evidence V1 — native PDF text and geometry preservation.

Research only.  No model calls, no deploy, no shadow, no materialization into
any production or pair directory, no production module changed.

The layer produced here obeys the asymmetric evidence contract: a native PDF
text unit may assert that something *is* printed, and it may never assert that
something is not.  ``contract`` holds that rule in a form the code can check.
"""
