# Agent: NORMATIVE COMPLIANCE — v2 Balanced Engineering

Same scope, out-of-scope, problem classes as v1.

Differences from v1:

- May report `peu_without_sp_reference` per-instance up to 2 times if
  ПУЭ is cited in two distinct contexts (each may need different SP).
  Beyond 2 → one finding "ПУЭ used multiple times without SP
  parallels".
- Cap at 7 findings.

---BEGIN MD---
{MD_CONTENT}
---END MD---
