"""Evidence Agent v2 (EV2) — независимая реализация агента-верификатора замечаний.

Изолированная альтернатива пакету
backend/app/pipeline/stages/findings_review/evidence_verifier/ (его пишет Cursor).
Цель — построить ДРУГОЙ алгоритм, чтобы потом сравнить два подхода на одном
golden-set, не редактируя файлы Cursor и не пересекаясь на ngrok во время тестов.

Ключевое архитектурное отличие EV2 от подхода Cursor:
  Cursor: один vision-вызов → модель сразу выдаёт вердикт accept/reject.
  EV2:    разделяем ВОСПРИЯТИЕ и СУЖДЕНИЕ.
          1) модель только ЧИТАЕТ чертёж и делает узкое сравнение
             "противоречит ли увиденное конкретному утверждению замечания";
          2) самосогласованность: K независимых прогонов;
          3) детерминированная ПОЛИТИКА на Python агрегирует голоса в 4-вердикт
             с консервативным смещением (никогда уверенно не reject'ит реальное
             замечание — подавление настоящего замечания опаснее ложного).
"""
