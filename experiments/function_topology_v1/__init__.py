"""FUNCTION TOPOLOGY V1 — from a graph of wires to a graph of functions.

Research only.  No model calls, no deploy, no shadow, no materialization, no
production module changed, nothing written next to a PDF, nothing pushed.

PDF EVIDENCE V2 (verdict **B**) proved that a schematic's connectivity can be
read from the native vector layer deterministically: 29 193 nodes, 32 802 edges
that name the drawn fact proving them, zero leakage across seven controls.  It
also proved why that did not move Function Lineage a millimetre, and the reason
was not accuracy — it was **granularity**:

    a Function Passport entity is a *switchboard*;
    a topology node is *one of its wires*.

So this package asks the next question and only that one: can the low-level
graph be aggregated **deterministically** into the thing an engineer names — a
board, a ВРУ, a riser, a branch of a system — and can that aggregate then be
bound to a FunctionScope by structure rather than by resemblance?

The low-level graph of V2 is treated as a **frozen reference**.  Nothing here
turns another line into a conductor, relaxes the hop rejection, the table-grid
rejection, the frame rejection, the underline rejection or the prohibition on
nearest-label ownership.  Coverage that is missing stays missing; this track is
not allowed to buy it.
"""
