"""audit-worker — отдельный устанавливаемый агент для стороннего VPS (этап 0).

Что он делает сейчас:
  регистрируется в центре → шлёт heartbeat с ресурсами → забирает БЕЗОПАСНОЕ
  тестовое задание `test_pipeline_v1` → скачивает и проверяет пакет →
  запускает фиксированный тестовый процесс → копит события в дисковом outbox
  и досылает их пакетами → собирает результат в TAR и грузит чанками.

Чего он НЕ делает и не должен на этом этапе:
  реального аудита, вызовов Claude Code / Codex, нормативного этапа,
  импорта backend.app.* (агент самодостаточен и ставится отдельно).

Главный принцип безопасности: центр НЕ передаёт ни shell-команду, ни имя
исполняемого файла, ни argv, ни путь, ни переменные окружения. Воркер строит
argv тестового процесса САМ (см. test_runner.build_argv), а все скалярные
параметры валидирует и зажимает повторно на своей стороне.
"""

__version__ = "0.1.0"
PROTOCOL_VERSION = 1
