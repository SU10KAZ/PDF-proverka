# Зависимости пакета audit_worker (этап 0).
#
# Отдельный файл, а не общий requirements.txt: агент ставится на СТОРОННИЙ VPS,
# где кода платформы нет вовсе. Он не импортирует backend.app.* — это
# проверяется тестом test_agent_does_not_import_backend.
#
# Первый аудит установил, что корневой requirements.txt неполон (нет PyMuPDF,
# openpyxl, Pillow, numpy). Здесь список собран по фактическим импортам пакета,
# а не по памяти — на этапе 0 он честно короткий, потому что реального аудита
# агент не выполняет.
#
# Установка:
#   python3 -m venv /opt/audit-worker/venv
#   /opt/audit-worker/venv/bin/pip install -r requirements-worker.txt

httpx>=0.27.0          # единственный сетевой клиент к центру (HTTPS, Range, стриминг)
psutil>=5.9.0          # снимок RAM/swap; опционален — без него слоты считаются по stdlib

# Компрессия пакетов. tar+gzip работает из stdlib и достаточен для этапа 0;
# zstd (ADR-002 техпроекта) станет дефолтом, когда поедут реальные пакеты
# проектов на сотни мегабайт. Раскомментировать вместе с включением zstd:
# zstandard>=0.22.0

# ЧЕГО ЗДЕСЬ НЕТ И НЕ ДОЛЖНО БЫТЬ НА ЭТОМ ЭТАПЕ:
#   PyMuPDF / openpyxl / Pillow / numpy — понадобятся, когда воркер начнёт
#   выполнять реальный конвейер (шаг 2+ разбиения из техпроекта §25);
#   anthropic / openai — LLM на воркере запускаются локальными CLI с ambient-
#   авторизацией в $HOME, а не SDK, и на этапе 0 не запускаются вовсе.
