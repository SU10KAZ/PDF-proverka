"""Межпроцессный E2E-стенд удалённого аудита.

Пакет собран так, чтобы им пользовались ОБА потребителя: pytest-тесты
(`tests/test_distributed_audit_e2e_*.py`) и воспроизводимый smoke-скрипт
(`scripts/smoke_distributed_audit_execution_e2e.py`). Дублировать сборку стенда
в двух местах — верный способ получить два разных стенда и не заметить этого.

Что здесь ЕСТЬ:
  * `fixture`   — синтетический проект в фактической раскладке `projects_v2`;
  * `isolation` — изолированное окружение (пустой HOME, вычищенные секреты,
                  свой PATH, сетевой guard);
  * `providers` — поддельные CLI-провайдеры как НАСТОЯЩИЕ подпроцессы;
  * `semantic`  — контракт эквивалентности local/remote и его нормализатор;
  * `harness`   — управление настоящими процессами (backend, agent, executor).

Чего здесь НЕТ и быть не должно: подмены `PipelineManager`, подмены
stage-runner'ов, моков HTTP. Подделывается ровно последний метр — внешние
LLM-провайдеры и тяжёлая нормативная зависимость.
"""
