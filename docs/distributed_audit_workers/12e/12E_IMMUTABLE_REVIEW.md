# Immutable adversarial review

Status: `PENDING`.

It must occur only after all remaining implementation and physical evidence is
committed. A detached read-only worktree at the exact candidate SHA will review
job loss/orphans, duplicate Executor/action ledger, EventOutbox, ResultAck and
retention, data-plane resume, cancel, mTLS/certificate failure, multi-slot,
ownership/rollback and production isolation. A defect requires a new commit,
targeted regression and a new review; this report cannot be pre-filled.
