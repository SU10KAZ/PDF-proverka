# 11D — развёртывание на пилотном воркере 176.12.77.31

## 1. Что развёрнуто

| поле | значение |
|---|---|
| source branch | `feat/distributed-audit-workers-text-analysis-provider` |
| source commit | `a0881dc57b5b59f07fc3812362deb55c4b370a2f` |
| pipeline revision | `git:a0881dc57b5b59f07fc3812362deb55c4b370a2f` |
| release | `20260809T172141-716cec362755` |
| tree hash | `sha256:716cec36275570eae66f424c0cc2f7261ded6f307706983022fc951449a66c5e` |
| archive sha256 | `71298997d773b9818ea594e39186a8a2315dc275f5dfbea2ae6329ef9731c43e` |
| файлов в артефакте | 570 (17,95 МБ сырьём, 3,4 МБ архивом) |
| deployed commit (проверено на .31) | `a0881dc57b5b59f07fc3812362deb55c4b370a2f` (из `MANIFEST.json` распакованного релиза) |

Артефакт собран штатным `scripts/deploy_audit_worker.py build` — тем же, что
использовался на 11B. Скрипт не изменялся. Он же проверил denylist: `.env`,
`.claude`, `.codex`, `.ssh`, `projects/`, `projects_v2/`, `knowledge_base/`,
ключи и токены в артефакт не попадают. `verify` на центре и повторная сверка
sha256 на воркере — совпали.

Ручных незакоммиченных правок на .31 нет: всё, что там исполняется, входит в
дерево коммита `a0881dc5` (кроме операторских файлов — политики моделей и копии
входного документа, см. §3).

## 2. Отдельный 11D-рантайм (§7 задания)

Существующая установка воркера **не тронута**. 11D живёт в собственном корне:

```
~/audit-worker-11d/
  app/20260809T172141-716cec362755/   ← распакованный артефакт
  current -> app/20260809T172141-716cec362755
  data/                               ← корень данных 11D (политика, разрешения)
  doc/                                ← ОДНА копия входного Markdown
  prompts/prompts_snapshot/           ← снимок промптов + профиль ЭОМ
  sandbox/<task_id>/                  ← каталоги попыток
  tools/                              ← раннер 11D и сборщик доказательств
  logs/
```

Состояние существующей установки на момент развёртывания (снято до прогона):

| что | значение |
|---|---|
| `~/audit-worker/current` | `app/20260809T140821-c1070168cda4` (код **11B**, commit `d21035d4`) |
| `worker.db` | 401 408 байт, mtime 1786256692 |
| `worker_state.json` | 403 байта, mtime 1786256798 |
| каталогов заданий | 37 |
| активных процессов `audit_worker` | 0 |
| systemd-юнитов воркера | 0 (ни user, ни system) |
| активных попыток / незавершённых загрузок | 0 |
| режим провайдера в `worker.env` | `fake` (`AUDIT_WORKER_ALLOW_REAL_LLM=false`) |

То есть 11D не выполняется поверх работающей попытки, а прежняя установка
остаётся на коде 11B со своим `worker.db`, своими заданиями и своим (поддельным)
режимом провайдера.

Python берётся из venv существующей установки (`~/audit-worker/venv/bin/python`,
3.12.3) — **на чтение**. Создавать второй venv значило бы ставить зависимости из
сети на чужую машину ради одного прогона; чтение чужого venv её не меняет.

## 3. Файлы, которых нет в артефакте, и почему

| файл | откуда | почему не в артефакте |
|---|---|---|
| `data/provider_policy.json` | написан оператором на .31, режим 0600 | это КОНФИГУРАЦИЯ МАШИНЫ, а не код: какой моделью пользоваться на этом VPS, решает его администратор (§10). В артефакте её быть не должно — иначе центр раздавал бы модели воркерам |
| `doc/133-23-ГК-ЭС_document.md` | одна копия входного документа | пользовательский документ в репозиторий и в артефакт не попадает (§15, §32) |
| `prompts/prompts_snapshot/` | `prompts/pipeline/` + `prompts/disciplines/{_registry.json,EOM}` | в бою профиль дисциплины приезжает пакетом ЗАДАНИЯ, а артефакт возит только `_registry.json` (правило `BUNDLE_INCLUDE`). В 11D центрального пакета нет, поэтому снимок разложен административно |
| `tools/run_11d_text_analysis_provider.py` | из коммита `a0881dc5` | скрипт входит в дерево коммита, но не в `BUNDLE_INCLUDE` (`scripts/` не возится на воркер). Скопирован отдельно как операторский инструмент |
| `tools/capture_proc.sh` | сборщик доказательств локальности вызова | вспомогательный инструмент прогона, кода платформы не касается |

## 4. Локальная политика моделей

`~/audit-worker-11d/data/provider_policy.json` (0600):

```json
{
  "policy_version": 1,
  "claude": {
    "auth_mode": "ambient_user",
    "capabilities": {
      "strong_audit": {
        "model": "claude-opus-5",
        "accepted_reported_models": ["claude-opus-5", "claude-opus-5[1m]"]
      }
    }
  }
}
```

Проверено на .31: `model_policy.load_policy(...)` возвращает
`claude-opus-5` и допустимые формы — без единого обращения к модели.

## 5. Ничего не обновлялось (§5 задания)

| компонент | обновлялся | версия на момент прогона |
|---|---|---|
| ОС | нет | Ubuntu 24.04.4 LTS |
| ядро | нет | 6.8.0-136-generic |
| Python | нет | 3.12.3 (`/usr/bin/python3` и venv) |
| Node | нет | v20.20.2 |
| Claude CLI | нет | 2.1.220 (Claude Code), нативный бинарь |
| Codex CLI | нет | 0.147.0 (в 11D не используется) |
| VS Code / VS Code Server / расширения | нет | не трогались |
| venv воркера | нет | использован на чтение |

Блокеров, требующих обновления, не возникло: код 11C+11D импортируется и
исполняется на текущем состоянии машины.

## 6. Репетиция без модели

Перед реальным вызовом путь пройден целиком в режиме `fake` (подставной CLI,
ноль обращений к модели) — дважды:

| где | результат | что доказано |
|---|---|---|
| центр 176.12.77.128 | PASS | код работает; промпт собирается; артефакт пишет конвейер |
| **воркер 176.12.77.31** | **PASS** | боевой раннер этапа на реальном документе: инструкции 18 018 симв., документ 11 544 симв., вычищено 2 файловые ссылки, 1 замечание записано конвейером, argv содержит `--model claude-opus-5`, `--tools=`, `--max-turns 1` |

Обращений к модели в репетициях: **0**.

## 7. Проверка каналов

SSH использовался только как административный канал: создание каталогов,
доставка артефакта (`scp`), распаковка, запись политики, снятие inventory,
запуск процесса. Inference в репетиции выполнял локальный процесс на .31
(подставной CLI, запущенный `ProviderAdapter` из того же процесса).
Продовый ingress центра не включался, `worker.env` существующей установки не
изменялся, ни одна продовая настройка платформы не трогалась.
