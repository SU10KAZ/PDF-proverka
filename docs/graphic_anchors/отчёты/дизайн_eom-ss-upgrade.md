All probes done. Composing the design map.

# Дизайн-карта: апгрейды зрелых дисциплин ЭОМ (вне однолинейки) и СС

**Эмпирика:** пробы в `/tmp/claude-1001/-home-coder-projects-PDF-proverka/6f655732-ccf4-4ad5-bf7d-95fbe65d0668/scratchpad/probe_eom_ss/` на 7 PDF корпуса (`experiments/блоки разных дисциплин/{ЭОМ,СС}/`). Сквозные факты, подтверждённые числами:

1. **Ключ `dashes` ВСЕГДА пуст** в AutoCAD-экспортах (проверено на планах 44578 и 37725 drawings) — штрихпунктир/пунктир существует только как цепочки коротких коллинеарных сегментов. План достройки из `vector-paths-drawings.md` п.3 («извлекать dashes») **нерабочий** — нужен примитив «сборка штрих-цепочек».
2. **Белые halo-дубли выносок**: каждая leader-линия продублирована белым штрихом (1.00,1.00,1.00) поверх/под чёрным (лотки 4KGV: белые диагонали 106-116pt рядом с чёрными 48-131pt). Белый цвет обязан фильтроваться во всех detect-примитивах.
3. **Красная подложка**: на структурной СКУД 9AJM красный = 82160 из 98874 сегментов (архитектурная подложка). Нужен фильтр «доминирующий цвет >60% = substrate».
4. **Grid-hash для T-junction работает**: O(N²)-проход из `build_segment_components` (vector_path_graph.py:491) заменён бакетами 4pt — 10532 сегмента СКУД обработаны за секунды.

---

## НОВЫЕ ПРИМИТИВЫ общей библиотеки (все — в `backend/app/pipeline/stages/block_grounding/vector_path_graph.py`)

| ID | Примитив | Эмпирическое обоснование |
|---|---|---|
| **P1** | `extract_leader_callouts_v2(page, text_lines, segments)` — генерализация `extract_callout_leaders` (:582-623): (а) убрать хардкод префиксов (:590) — принимать ЛЮБУЮ строку текст-слоя, у bbox которой (окно X±16pt, Y −8..+12pt) есть конец чёрного диагонального сегмента ≥10pt; (б) детект полочки — горизонталь в полосе Y=[низ строки −3, +8] с X-перекрытием; (в) цель = ближайший сегмент любой компоненты у дальнего конца (не только vivid); (г) фильтр белых halo | Лотки СС 4KGV: 10/10 выносок связаны, дальний конец ≤4.3pt от цели, цвет цели = система (СБ→синий, СПЗ→красный, гильзы→магента); полочка 93.8pt подтверждена. Трассы ЭОМ 7XG4: «Лоток ОДН/Перф.400х100» — полочка+диагональ, цель 1.9pt синий; «Лоток ПЭСПЗ» 0.7pt. Молниезащита 64JV: «токоотвод» ×6 — конец leader 0.8-5.7pt от текста |
| **P2** | `build_dash_chains(segments, gap_max=8.0, dash_len=(2,20))` — сборка коллинеарных штрих-цепочек в логические линии (сохранять цвет/width, счёт штрихов) | Молниеприёмная сетка 64JV: красные width=1.2 штрихи 14-17pt с пробелами 7.0-7.2pt; 13 горизонтальных цепочек собраны при gap≤8; 219 штрихов len=14 + 223 len=1 |
| **P3** | `detect_marker_circles(page, dia=(6,40))` — обобщение `detect_terminal_ellipses` (:110-170) за пределы окна 7-10pt: drawings из 2-4 `"c"`-items, near-квадратный bbox; + `token_inside` (ровно одно слово в bbox) | План СС 4EVJ: 69 окружностей, из них **36 маркеров помещений Ø17.0pt** с кодом внутри (`1.Т.5`, `2.МОП.2`, `С.А.2`); молниезащита 64JV: 30 кружков Ø17 (осевые марки) + 6 Ø3 |
| **P4** | `cluster_by_gap(xs, threshold)` — вынести 3 инлайн-копии из singleline_graph_geometry (G:1139, 1266, 1377) | — |
| **P5** | Пространственный индекс (grid-hash, ячейка 4pt) внутри `build_segment_components` вместо O(N²) (:491-506) | СКУД 10532 сегмента — union-find+T-junction за секунды; планы по 37-49К сегментов иначе неподъёмны |
| **P6** | `attach_labels_to_components(labels, comps, limit)` c **пер-экземплярным** учётом (не по имени: `Y1.1` повторяется в каждом дверном комплекте СКУД) и порогами из профиля | Освещение 4EMR: 119/119 меток групп ≤28pt (med 9.6, p90 24.2, max 26.8); СКУД: 195/201 ≤15pt |
| **P7** | Цветовые фильтры профиля: `exclude_colors` (белый halo), `substrate_color` (авто: цвет с долей >60% сегментов) | п.2-3 выше |
| **P8** | `bind_label_over_line(label_bbox, segments, dy_max=8)` — «подпись над своей линией»: горизонтальный прогон под bbox с X-перекрытием | Клеммник СОВ 79PP: 12/12 кабельных марок на 3.3-3.7pt над горизонтальным проводом 99-154pt |
| **P9** | `calibrate_scale(axis_tokens, dimension_words)` — pt→мм по паре повторов осевого токена (каждая ось `П.4`/`1.А` встречается ровно 2× — оба конца) + размерному слову между осями | 64JV и 4EVJ: все оси ×2; `8400` (мм) присутствует на обоих планах |

---

### `electrical_distribution_plan` — планы силовых сетей ЭОМ (27П/2О) и `lighting_plan` — планы освещения (37П/0О)

- **ЯКОРЯ (текст):** существующие `_CIRCUIT_RE`/`_PANEL_RE` (electrical_geometry.py:57-64) + НОВОЕ: `гр.\d` в нижнем регистре (4EMR: 20× `гр.3.1` — текущий `Гр.N` его ловит только case-insensitively — проверить флаги), составные аннотации `К{корп}.{секц}.{группа}[-фаза]` (`К3.1.1-2`, `К3.1.1а-2` — суффикс «а» = аварийное освещение), `ЯТП|С\.А\.\d` (ящики/светильники аварийные).
- **ЯКОРЯ (графика):** vivid-сегменты трасс — **цвет = тип сети**: на 4EMR синий (0,0,1) 1316 сегм. = рабочее освещение (72 метки), красный (1,0,0) 945 = аварийное (42 метки); подтверждено разложением 119 меток по цветам ближайшего сегмента.
- **ПРАВИЛА СВЯЗЫВАНИЯ:**
  1. P6: метка группы → vivid-сегмент, лимит 28pt (покрытие 100% на 4EMR);
  2. P5-компоненты vivid по цветам (tol 0.7 + T-junction): магистральный ствол собрал **60 меток групп И панели ЯТП/С.А.1 на одной компоненте**; 62/119 меток геометрически доходят до панели за один проход;
  3. остаток закрывается **семантикой кода**: `К3.1.* → ВРУ-К3.1` (декод корпуса/секции из префикса — приём `_encoded_building` alia_scheme_geometry.py:215-226, перенести);
  4. сечение→кабель: кабельные марки вдоль трасс вешать P8 («подпись над линией»), не nearest.
- **ГДЕ МЕНЯТЬ:** `_build_plan` electrical_geometry.py:134-162 — сейчас `_attach(limit=28)` даёт узлу только `route_id`; добавить (2)-(3): рёбра `group_label→network→panel` c `edge_state: colored_path_confirmed | semantic_panel_prefix`.
- **FINDINGS:** группа на плане без трассы / трасса без метки группы; группа, чей код не match ни одной панели листа (пересортица); метка одной группы на компонентах разных цветов (рабочее ↔ аварийное перепутаны) — класс норм СП 256.1325800.2016 (аварийное освещение, разделение сетей), ПУЭ гл. 6; кросс-сверка с Вектографом однолинейки: группа с плана отсутствует в фидерах щита.
- **ГЕЙТ:** attach_rate ≥90% меток; ≥1 компонента с меткой И панелью; иначе режим `label_inventory`.
- **ПРИОРИТЕТ/ЭФФОРТ: ВЫСОКИЙ / средний** (все примитивы готовы или из ярусов A/B; связка «группа→линия→щит» — главный запрос аудита планов).

### `cable_route_plan` — планы кабельных трасс ЭОМ (11П)

- **ЯКОРЯ (текст):** `Лоток|Перф\.|короб|\d{2,3}х\d{2,3}` (уже в `_SIZE_RE`); **ЯКОРЯ (графика):** полочка+диагональ выноски (эмпирика: shelf=1, diag=1, цель 0.7-1.9pt на 7XG4).
- **СВЯЗЫВАНИЕ:** P1 выноска→цветная компонента трассы; типоразмер лотка становится атрибутом СЕГМЕНТА трассы, а не инвентарной строкой. Метрика длины компоненты (P9-масштаб) ↔ сверка с ведомостью.
- **FINDINGS:** участок трассы без типоразмера; смена сечения лотка вдоль одной компоненты без выноски; (с Этапом 01) заполняемость лотка по кабельному журналу — СП 6.13130, ГОСТ Р 50571.5.52.
- **ГЕЙТ:** ≥80% выносок связано (callout_link_rate уже считается в тру-грамматике лотков СС — переиспользовать).
- **ПРИОРИТЕТ: средний / низкий эффорт** (чистое применение P1).

### `lightning_grounding_plan` — молниезащита/заземление ЭОМ (11П)

- **ЯКОРЯ (текст):** существующие :193-196 + `FeZn-25х4|d8|токоотвод|h=[+-]?\d`; **ЯКОРЯ (графика):** молниеприёмная сетка = **штрих-цепочки** (P2: красный width 1.2, штрих 14-17pt/пробел 7pt — 64JV), кружки осей Ø17 (P3, 30 шт.), leader к «токоотвод» (P1, 0.8-5.7pt).
- **СВЯЗЫВАНИЕ:** 1) P2 собирает цепочки сетки; 2) P9 калибрует pt→м по осям (`П.2…П.6`, шаг из размерных слов `8400`); 3) интервалы между параллельными цепочками → **фактический шаг ячейки в метрах**; 4) P1 вешает «токоотвод FeZn-25х4» на конкретные вертикальные цепочки; счёт токоотводов по периметру.
- **НОВОЕ:** только P2+P9 (P1/P3 общие).
- **FINDINGS (детерминированные!):** шаг ячейки сетки > 6×6/10×10/12×12 м для уровня защиты — СО 153-34.21.122.2003 п.3.3.2.3 (+ параллельно ГОСТ Р МЭК 62305-3); среднее расстояние между токоотводами по периметру > нормируемого (10/15/20 м по уровню); сетка не покрывает участок кровли (bbox сетки vs контур). Это прямой аналог `_deterministic_protection_check` Вектографа — «пересчёт домена → готовые факты в промпт».
- **ГЕЙТ:** масштаб откалиброван (≥2 осевых пары + размер) И ≥4 цепочек сетки; иначе `mesh_inventory` без метрических findings.
- **ПРИОРИТЕТ: ВЫСОКИЙ / средний** — единственный профиль, где граф даёт готовое численное нарушение нормы без LLM.

### `cable_tray_axonometry` (СС, tray_axonometry — сейчас inventory_only)

- **ЯКОРЯ:** `_TRAY_RE`/`_SLEEVE_RE` (low_voltage_geometry.py:50-61) остаются; графические — P1-выноски вместо **zip по порядку** (`_build_tray_graph` :453-469 — главная ложь слоя: zip держится на совпадении порядка).
- **СВЯЗЫВАНИЕ:** 1) P1: каждая выноска → чёрный leader → дальний конец → ближайший цветной сегмент (эмпирика 10/10, ≤4.3pt); 2) цвет цели = система (СБ=синий 6406 сегм., СПЗ=красный, гильзы=магента 4KGV) — кросс-чек «текст СПЗ ↔ цвет красный», расхождение = GEOMETRY_CONFLICT; 3) P5-компоненты цветных сегментов → **топология сегментов лотка** (стыки, повороты, длина компоненты), выноска аннотирует свою компоненту; 4) `L=…м` из текста ↔ геометрическая длина компоненты × масштаб (P9) — допуск 15%.
- **ГДЕ МЕНЯТЬ:** low_voltage_geometry.py:428-498 (`_build_tray_graph`): заменить zip на P1-binding, снять `topology_state: visual_unverified` (:489) при link_rate=100%; обновить tray-ветку незакоммиченной `normalize_low_voltage_graph` (:632-835) — узлы-callout получают `network_id`, edges `annotates_segment`.
- **FINDINGS:** выноска без геометрии/геометрия без выноски; сумма L= по системе ↔ ведомость; лоток СПЗ и СБ в одном пучке без перегородки (совместная прокладка НС и СПЗ) — СП 6.13130.2021 п.6.4-6.5, ГОСТ Р 50571.5.52.
- **ГЕЙТ:** callout_link_rate ≥90% И у ≥80% выносок цвет цели согласован с системой из текста → `topology_confirmed`; иначе текущий `inventory_only`.
- **ПРИОРИТЕТ: ВЫСОКИЙ / низкий** — примитив P1 подтверждён на обоих корпусах, правка локальная.

### `low_voltage_terminal_wiring` (СС, terminal_wiring — cable_state=not_extracted)

- **ЯКОРЯ:** клеммные овалы (уже есть, :110-170), `_TERMINAL_LABEL_RE` (:237); НОВОЕ — кабельные строки `КСПВ…|U/UTP…` как якорь **над линией**.
- **СВЯЗЫВАНИЕ:** 1) P8: марка кабеля → горизонтальный прогон в 3.3-3.7pt под ней (12/12 на 79PP) → path-компонента прогона → `network.cable` (закрывает `cable_state="not_extracted"` vector_path_graph.py:414); 2) **multi-terminal disambiguation**: внутри `multi_terminal_review`-сети разрезать по кабельным привязкам — два прогона с разными марками = две логические цепи одной физической шины; клеммы, чей стык (радиус 4.8pt :346) объединил компоненты, наследуют обе; 3) счётчик жил из марки (`1х2х0,97` = 2 жилы) ↔ число клемм пары — детерминированный чек.
- **ГДЕ МЕНЯТЬ:** `build_terminal_wiring_topology` vector_path_graph.py:277-452 (добавить cable-binding перед классификацией сетей :388-391); `_build_terminal_graph` low_voltage_geometry.py:559-591.
- **FINDINGS:** сеть без марки кабеля; марка не соответствует типу цепи (RS-485 без витой пары; ШС АПС не нг(А)-FRLS/HF) — СП 6.13130, СП 484.1311500.2020; жильность < числа подключений.
- **ГЕЙТ:** поднять `confirmed_pair`-порог не надо — добавить `cable_bound_rate ≥70%` для перехода из `requires_path_tracing` в `partial_topology`+.
- **ПРИОРИТЕТ: ВЫСОКИЙ / средний** — прямо назван в `required_next_layer` самого модуля (:624).

### `discipline_floor_plan` (СС, 20 ограниченных — топология трасс)

Причины из ss_out и их закрытие:
- «не построена координатная сетка» (14): `_plan_axes` (alia_remaining_geometry.py:88-102) требует ≥2 повторов токена — кропы часто режут один конец оси. **P3**: осевой/П.-токен + кружок Ø17 + длинная линия через центр = ось с ОДНОГО экземпляра. Эмпирика 4EVJ: все оси `П.х` ровно 2×, кружки Ø17 есть;
- «не найдены три фактических подключения шкафов к трассам» (16): подпись ОСПД лежит в **114-167pt** от vivid — nearest не сработает никогда; но это выносная аннотация → **P1-leader** дотягивает её до трассы; PoE-метки у камер уже в 4.8-7.1pt (прямая привязка P6 лимитом 18 работает);
- «не построен граф трасс» (5): P5 grid-hash + P7 substrate-фильтр на планах 37-49К сегментов;
- «камеры не связаны со шкафами» (2): составные аннотации `ОСПД2.1-ВК2.1.1.1-…` на плане — парсить как аннотационную пару (приём `_RS485_PAIR_RE` structural_access_geometry.py:38-45).
- **БОНУС:** 36 кружков-помещений Ø17pt с кодами (`2.МОП.2`) → материализовать помещения как circle-anchored узлы вместо `polygon_state: "label_only"` (:315) — покрытие помещений трассами/устройствами становится проверяемым (СП 484 зоны защиты).
- **ГДЕ МЕНЯТЬ:** `_build_plan` alia_remaining_geometry.py:291-350.
- **ГЕЙТ:** оставить текущие критерии — они начнут проходить; добавить режим `axis_from_circles`.
- **ПРИОРИТЕТ: ВЫСОКИЙ / средний-высокий** (крупнейшая группа ограниченных).

### `skud_structural_site` (СС, structural_access — networks=[], edges=[])

- **ЭМПИРИКА 9AJM:** после исключения красной подложки (P7) и белого — 10532 сегмента, 326 компонент; **195/201 меток устройств привязано ≤15pt**; компоненты = дверные комплекты: `[MD-контакт, Y-замок, К1..К5-кабельные метки]`, 8/8 верхних компонент содержат и устройство, и кабельные линии.
- **СВЯЗЫВАНИЕ:** 1) P7+P5+P6 → компоненты «дверной комплект»; 2) метки `К\d` на линиях = номерованные кабельные линии контроллера — семантическое ребро комплект→STR-контроллер по номеру линии; 3) полноту комплекта проверять составом (считыватель+замок+кнопка+контакт).
- **ГДЕ МЕНЯТЬ:** structural_access_geometry.py:696 (`"networks": [], "edges": []` СКУД-ветки) + `_color_metrics` (:481-493) перестаёт быть только диагностикой; добавить `from_source`-вход (его нет — :713-734 принимает только 1-страничный кроп).
- **FINDINGS:** дверь/точка доступа без контроллера; комплект без замка или считывателя; устройство без кабельной линии — ГОСТ Р 51241-2008, СП 132.13330.
- **ГЕЙТ:** device_attach_rate ≥90% (эмпирика 97%) И ≥3 комплектов с контроллерной линией → снять `topology_state: visual_unverified` (:706).
- **ПРИОРИТЕТ: средний / средний**.

### Ограниченные/кандидаты остальных СС-профилей (52) — маршрутизация по примитивам

- `device_terminal_wiring` ×17 («<80% клемм привязано», «нет пар») → P5 (индекс позволит поднять радиус стыка без квадратичной цены) + P8 кабель-байндинг + овалы P3; ожидаемый переход ≥10 блоков в полные;
- `physical_panel_layout` ×15 («неполный состав щита») → P1-выноски позиций к юнитам + P3 кружки-позиции; состав собирается из выносок, а не только словаря;
- `external_terminal_wiring` ×5 кандидатов (фрагменты 3<10 устройств при complete=True) → **не геометрия, а гейт**: добавить режим `fragment_inventory` в `_GATE_RULES` (alia_scheme_geometry.py:901-916) — мин. 3 устройства + attach 100% вместо абсолютного ≥10;
- `cctv_floor_network` ×2, `automation_control_hierarchy` ×2, `dispatch_integration_backbone` ×1 → P1-leader для дальних подписей + P5 на плотных листах (141915 paths — только с grid-hash).

### Вычистка ЭОМ-хардкодов Вектографа (ярус C → конфиг профиля)

Вынести в декларативный конфиг `backend/app/pipeline/stages/block_grounding/block_context/reference_catalog/` (механизм уже есть: `load_reference_rules()` читается в `_catalog_clip_margin`, singleline_graph_geometry.py G:113) — новый раздел `singleline_dialect`:
1. `_extract_power` G:512-523: «ВА-305 320А 35кА», «ВР-101-630», «АВР-301 40А», «Мосэнергосбыт» → словарь `incomer_device_patterns` + `external_source_label`;
2. `_TT_ANCHOR_RE` G:769 (имена конкретных панелей «РП5.1 (ПЭСПЗ)») → generic-паттерн `панель+(аббревиатура)` с per-project extension;
3. `_extract_service_elements` G:977-1028 (if-каталог ОП101/НАРТИС/НПН2/МК103/ШУ-Л) → таблица `service_element_catalog: [{pattern, class, attrs}]`;
4. `_extract_notes` стоп-маркеры G:964 («НАРТИС-И300», «Инв. № подл») → `note_stop_markers`.
Риск низкий: чистый рефакторинг данных, поведение фиксируют существующие тесты Вектографа; сначала снять снапшот графов 15 боевых однолинеек и сверить бинарно после выноса.
**ПРИОРИТЕТ: средний / низкий** — обязательный шаг перед переносом Вектографа на другие объекты (не-ALIA альбомы эти хардкоды не пройдут).

### Остальные профили ЭОМ (компактно)

- `panel_circuit_scheme` (15П): уже имеет confirmed_pair (electrical_geometry.py:177-178); апгрейд P1 (выноски номиналов) — низкий приоритет;
- `equipotential_scheme`/`ozds_topology`/`electrical_installation_detail`: P1-выноски размеров к элементам узла (общий разрыв «размер→элемент» — зона других агентов, примитив тот же P1);
- `electrical_equipment_drawing`: P3 кружки-позиции + P1 → состав щита из выносок;
- `illuminance_calculation`: без изменений (честный `uncalibrated_vector_field`);
- `electrical_site_overview`: P9-калибровка для длин питающих линий — опционально.

### Порядок внедрения (ценность × зависимость)

1. **P5 grid-hash + P7 фильтры** (разблокирует всё на больших листах) → 2. **P1 leader v2** (лотки СС + трассы ЭОМ + floor_plan ОСПД + panel_layout) → 3. **P8 cable-over-line** (клеммники) → 4. **P2+P9 молниезащита** (детерминированные findings) → 5. группы→щиты на планах ЭОМ → 6. P3 кружки (оси/помещения/позиции) → 7. СКУД-комплекты → 8. вынос хардкодов Вектографа → 9. рекалибровка гейтов (`fragment_inventory`, `topology_confirmed`).

Все правки — поверх незакоммиченной ветки `feature/block-vector-graphs` (новые СС CTX-профили + `normalize_low_voltage_graph` + sidecar-галереи): апгрейды лотков/клемм обязаны обновлять и normalize-проекцию (nodes/networks/edges/readiness), иначе CTX-окно не увидит новую топологию.