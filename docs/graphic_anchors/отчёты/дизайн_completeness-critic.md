Both disputes are empirically confirmed. Composing the critique.

---

# КРИТИКА ПОЛНОТЫ: дельты и противоречия 8 дизайнов

Спот-пробы критика: `/tmp/claude-1001/-home-coder-projects-PDF-proverka/6f655732-ccf4-4ad5-bf7d-95fbe65d0668/scratchpad/probe_critic/probe_disputes.py` (2 спорных факта перепроверены числами, см. §2.e, §2.f).

## 1. ПРОПУЩЕННЫЕ ТИПЫ БЛОКОВ (сверка со 105 профилями каталога)

Покрытие почти полное: АР 21/21, КЖ 6/6 (+2 новых табличных), КМ 7/7, ГП 13/13, ТХ 8/8, ОВ 9/9, ВК 11/11 (+water_supply_scheme), ЭОМ 12/12, таблицы — новый слой. **Дыры:**

1. **`installation_assembly` (СС, 37 полных + 1 кандидат — крупнейший СС-профиль!) не имеет дизайн-карты ни у кого.** При этом geom-ss-lowvoltage.md §3(д) прямо называет `_build_install` «главным незакрытым пунктом для размер→полочка→элемент». Монтажные узлы СС — прямой потребитель dimensions/leaders (аналог hvac_installation_detail из ov-vk-upgrade). Нужна карта (механика 1:1 от ОВ-узлов: P5/P6/P10 в терминах ov-vk).
2. **`access_point_assembly` (3) и `multidiscipline_niche_layout` (1)** — сборки СКУД-дверей с позициями-цифрами и размерами `\d{3,4}мм`, ниши с гильзами `Ду=…мм` — естественные потребители leader/dimension, не упомянуты.
3. **Зрелые СС-профили без апгрейда** (voice_alarm, fiber_ring, metering_*, lift_dispatch, mgn, cabinet_*, functional, sov_structural_*, fire_alarm_loop_topology) — вероятно осознанно, но в итоговом документе нужен явный список «no-change by design», иначе неотличимо от «забыли».
4. **Штамп и «Общие данные»/примечания** — вне 105 профилей; tables-design исключает штамп как анти-якорь, но никто не фиксирует, что стамп-парсинг (лист/ревизия) остаётся в stage_comparison. Зафиксировать out-of-scope явно.
5. Реестр (infra-registry-router.md) для ВК содержит `control_circuit_graph` в PROFILE_LABELS, а `water_supply_scheme`/`pump_performance_chart`/`roof_drain_detail`/`fire_control_scheme` в цитированном перечне отсутствуют — при регистрации новых слоёв сверить PROFILE_LABELS с фактическими профилями water_geometry (ov-vk их проектирует, но регистрацию не упоминает).

## 2. ПРОТИВОРЕЧИЯ МЕЖДУ ДИЗАЙНАМИ

**a) Четыре разных «дома» для одной библиотеки.** primitives-layer: пакет `graphic_primitives/` (7 модулей); integration-design: тот же пакет, но **другой состав** (14 модулей, включая text_extract/columns/envelope); ar-design и kj-km: `dimension_geometry.py`; tx-gp: `construction_anchor_primitives.py`; eom-ss и ov-vk: всё инлайн в `vector_path_graph.py`. Если исполнять буквально — три копии каждого детектора. Решение: канон = пакет integration-design (он же покрывает shims/тесты), остальные имена отменить; primitives-layer's core.py влить в paths.py+spatial.py.

**b) P-нумерация несовместима.** «P1» = 4 разных примитива (АР: dimension_bindings; КЖ: clip; ЭОМ/СС: leader v2; ОВ/ВК: glyph-слова). Итоговому документу нужна таблица соответствия P-имён → канонические имена функций.

**c) Пять leader-детекторов, четыре модели размера — и ни один дизайн не признаёт чужие моды.** Модели привязки числа: (1) **в разрыве линии** — главный режим АР (81/100 привязок на 6QT6!), есть ТОЛЬКО у ar-design; (2) над серединой интервала/колена (primitives-layer, КЖ-полилиния); (3) на полочке под числом (kj-km P4, 140/321); (4) в пролёте между засечками (tx-gp dim_chains). Унифицированный детектор обязан поддерживать все четыре как режимы, иначе на АР теряется главный сигнал. Допуски полочек тоже расходятся: зазор ≤4.5 pt (primitives) vs y1..y1+6 (АР) vs 0.2–3.4 (КЖ) vs −3..+8 (ЭОМ).

**d) Засечки: «стабильно 4.07 pt во всех дисциплинах» (primitives Ф2) опровергается остальными.** АР — бимодально 1.4–2.5 И 4.0–6.8; КЖ — моды 2.9/3.1/4.1; ТХ — 1.8–2.9; ОВ/ВК — 1.2–5. Диапазон primitives-layer (2–6.5 @35–55°) режет нижнюю моду АР и ТХ 1.8. Унификация: 1.2–6.8 pt @30–60° + пресеты в конфиге профиля.

**e) Кружки: диапазоны диаметров взаимоисключающи — ПРОВЕРЕНО.** primitives-layer: Ø20–40; ar-design: Ø11.9 и «кольцо из ~36 l-сегментов R15.7» (представление, отсутствующее у всех остальных); КЖ: классы 5.5–7.2/22.7–25.6/45.2 + концентрические болты; ТХ: 13–26; ЭОМ/СС: 6–40; ОВ: 15–30. Моя проба на том же 7DU7 (АР): all-`c` квадратные drawings дают пики **6.6(×32), 9.7(×29), 11.9(×23), 13.3(×29), 16.9(×30), 24.4(×80), 27.8(×116), 28.4(×39), 45.9(×14)** — на ОДНОМ листе живут все классы 4–55 pt; окно 20–40 теряет и позиции, и марки узлов. Нужен один `detect_circle_marks(Ø 4–55)` с кластеризацией по классам + 4 представления (full_arc / paired_halves / l-ring / polygon_approx) и фильтром «ровно 1 токен внутри» (у меня: 44 подтверждённых).

**f) «dashes мёртв во всём корпусе» (primitives Ф1) — фактически неверно, ПРОВЕРЕНО.** График насоса 4KMQ: `'[ 2 ] 2'` × 20 drawings (пунктир рабочей точки — семантически важнейшие линии графика, ov-vk факт 4). Формулировка для документа: «мёртв в CAD-чертежах, жив на графиках характеристик; извлекать всегда».

**g) Фаза 0: прямой конфликт контрактов.** integration-design: «Фаза 0 = чистый рефакторинг, 0 поведенческих изменений, бинарное сравнение корпуса 983/124/26». primitives-layer: в Фазу 0 включены «обязательные фиксы» rotation-нормализация (Ф7) и fail-closed crop-клип (Ф6) — а это меняет counts на десятках блоков (19–65% drawings вне кропа по КЖ/АР/ТХ). Одно из двух: либо фиксы уходят за флаг в Фазу 1, либо baseline пересъёмка — сейчас оба текста несовместимы.

**h) Rotation: два противоположных рецепта.** primitives Ф7: drawings нормализовать через `rotation_matrix` к повёрнутому пространству текста. ov-vk факт 9 (профиль 7VQA, rot=270): слова и drawings в ОДНОМ raw-пространстве, дератирование любого слоя поодиночке ломает матчинг (raw 10/23 vs derot 5/23 vs rot 0/23). Обе стороны с числами → поведение зависит от метода (`get_text("words")` vs `"dict"`) и версии PyMuPDF. Нужен один канонический хелпер `normalize_spaces(page)` с самопроверкой (контрольный матч токен↔drawing) вместо двух прописанных констант.

**i) Таблицы: 5 реализаций + конфликт владения блоками.** Grid-детектор предложен в primitives (tables.py), ar-design (P6), kj-km (P10), tx-gp (line_grid), tables-design (table_geometry.py — самый полный). Хуже: **ведомость дверей и ведомость отделки закреплены одновременно за двумя дизайнами** (ar_opening_drawing/ar_finish_plan у АР vs door_window_schedule/finish_schedule у tables-design), спецификации КЖ — за kj_steel_schedule (внутри structural_geometry, «ПЕРЕД графическими») и за table_structured (в роутере «ДО всех дисциплинарных профилей»). Findings дублируются (Σ по этажам vs Всего, EI — в обеих картах). Решение: табличный движок = библиотека, вызываемая дисциплинарным профилем (profile_id сохраняется → сигнатуры каталога стабильны); router-ветка `table_structured` — только fallback для блоков без дисциплинарного профиля.

**j) Словарь доказательности: три несогласованных.** integration §2.2 — канонический реестр tier'ов поверх существующих кодов; primitives quality.py — свой словарь `confirmed_geometry|text_only|ambiguous|not_extracted`; ov-vk вводит `ocr_from_vector_glyphs`, которого нет в реестре integration. Свести к одному реестру (integration) + дописать новые коды (`ocr_from_vector_glyphs`, `dimension_break_confirmed`, `axis_ring_confirmed`).

**k) Конфиг вычистки ЭОМ-хардкодов: два адреса.** eom-ss: `reference_catalog/` раздел `singleline_dialect`; integration: `reference_catalog/profile_rules/EOM.json`. Один выбрать.

**l) Роутер: конфликт процессов.** integration (риски): «НЕ трогать каскад роутера до влития СС-ветки feature/block-vector-graphs». tables-design: «новая ветка в resolve_block_package между Вектографом и electrical_geometry». kj-km: правки classify_kj/km в structural_geometry (не роутер — ок). Нужен порядок: влитие СС-ветки → потом табличная ветка.

**m) Масштаб: per-sheet (АР P9) vs per-view (КЖ P6).** Эмпирика КЖ решает спор: мультивидовый 4HH4 даёт 60% инлайеров на per-page масштабе (смесь видов). Канон — per-view кластеризация с fallback на лист; АР P9 переформулировать.

**n) Мелкие дубли:** grid-hash ×4 (cell 4 vs 8 pt); cluster_by_gap ×3; «подпись у линии» ×3 (reading_line_pair / bind_label_over_line / bind_label_to_parallel_segment — унифицировать как семейство label_binding с режимами over/parallel/same-row); стрелки ×4 (fill-треугольник АР/ОВ, штриховой вал ГП, V-пара гидравлики — семейство поверх FillGlyph); пилот КЖ: волна 1 = embedded_parts (kj-km) vs Фаза 3b = reinforcement_plan (integration).

**o) tables-design `cross_block_edges` пишутся в ОБА пакета постфактум** — ломает иммутабельность hard-link sidecar-графов галереи (`_graphs/` из build_vector_graph_gallery.py) и идемпотентность артефактов block_context. Кросс-рёбра должны жить в отдельном version-артефакте, не мутировать пакеты.

## 3. НЕРЕАЛИСТИЧНОЕ / НЕДООЦЕНЁННОЕ

1. **Бюджет ~3с/блок vs 612 534 l-сегментов (WYYH, ВК)** с медианной длиной 0.0 (расплющенные кривые/штриховки): создание 600К Stroke-датаклассов — сотни МБ и секунды только на аллокацию. Обязательны: пре-агрегация микросегментов ДО датаклассов, стратифицированный отбор по width (уже предложен ar-design для фундамента 694C — сделать общим), per-класс капы.
2. **Детекторы прогоняются ДВАЖДЫ на блок**: block_context пишет prepared-пакеты, но Stage 02 вызывает `resolve_block_package(prefer_prepared=False)` (gemma_findings_only.py:1970). С тяжёлыми детекторами это удвоение стоимости всей стадии. Никто не флагирует. Нужна политика prepared-reuse (например, mtime/config-hash гейт).
3. **ov-vk P1 (glyph-слова → OCR кропов)**: движок OCR не назван. Qwen → не «офлайн/0 токенов»; tesseract → новая зависимость, а обведённые CAD-шрифты (ISOCPEUR) — ровно тот случай, где OCR традиционно валится (CLAUDE.md). ~35 блоков заложены на непроверенное звено — нужен отдельный OCR-пилот с метрикой ошибок ДО включения в план.
4. **«Замкнутый контур вокруг точки»** (tx_lift_plan шахты, gp building_footprint, АР P8-полигоны) — face-extraction из сегментного супа: ни одной пробы ни у кого, сложность недооценена всеми тремя потребителями. Вынести в отдельную фазу с явным fallback (точка-заглушка как сейчас).
5. **Нормы в findings-картах не верифицированы**: конкретные пункты (СП 113.13330.2023, ГОСТ 33984.1 таблицы, СО 153-34.21.122 п.3.3.2.3 и т.д.) даны по памяти агентов. Только integration-design оговаривает верификацию через norms-MCP — сделать это обязательным правилом всех карт. Плюс пропущенный класс ложняков: **DIM_SCALE_OUTLIER обязан исключать размеры со «*»** (не в масштабе, primitives-layer сам ловит `250*` регексом, но в findings-логике исключения нет).
6. **Вся эмпирика — один объект (ALIA), один CAD-диалект.** Полочки 0.2–1.2 pt, белые halo, глиф `▐`, засечки 45.0° — константы одного экспорта. До раскатки нужен валидационный корпус второго объекта (портал уже отдаёт вектор-PDF по crop_url — ПД-00542664 готовый кандидат).
7. **Гейт кладочного (link-rate ≥0.55) не проходит собственную эмпирику**: 7DU7 = 46% (числа = ряды кладки). Либо анти-паттерн «N ряд» до гейта, либо порог — иначе половина пилотных кладочных честно выпадет и пилот покажет ложный провал.

## 4. ПРОПУЩЕННЫЕ СКВОЗНЫЕ ВОПРОСЫ

1. **Кросс-блочный реестр версии — нужен четырём дизайнам, не спроектирован ни одним как общий механизм**: сечения план↔разрез (КЖ), «Линия N.N» план↔профиль (ГП), марка насоса кривая↔схема (ОВ/ВК), позиции таблица↔чертёж (tables). block_context — per-block стадия; нужен пост-pass с контрактом: отдельный артефакт `_output/cross_block_registry.json`, идемпотентность, findings с `related_block_ids`, инвалидация при пересборке любого блока.
2. **Версионность/инвалидация**: смена флагов GRAPHIC_PRIMITIVES_* не инвалидирует prepared-пакеты и sidecar-галереи; протокол bump'а catalog_version при изменении сигнатур (новые `*_total`) прописан только у integration; baseline DISCIPLINE_COVERAGE намеренно сдвигается в фазах >0 (limited→complete) — протокола пересъёмки нет.
3. **Промпт-бюджет**: задан только integration (~5–6К). Рендер таблиц без cap: кабельный журнал 17×9 и спека 20×9 = сотни ячеек; ведомость отделки 211 площадей. Нужны render_budget per-profile + row-cap с счётчиком излишка.
4. **Двойной маршрут детерминированных findings**: «факты в промпт» (Вектограф-паттерн, primitives Фаза 3) vs «детектор-инжект в merge» (tables-design, в духе run_protection_table_detector). Критерий выбора не задан; плюс не спроектирован дедуп с LLM-findings (та же арифметика спецификации найдётся и текстовым Этапом 01).
5. **Обвязка, которую все забыли**: `localize_profiled_block_graphs.py` пере-рендерит markdown по source_kind — новые слои/рендеры надо добавить туда, иначе локализация молча потеряет секции; фронтенд `profiled_graph_display`/панель «Граф схемы» — русские подписи новых kind заявлены (integration), но отображение слоёв dimensions/leaders в UI не спроектировано.
6. **Две таксономии профилей**: tables-design использует `stage_comparison/graphic_profiles.py` как условие входа, маппинг между таксономиями block_grounding и stage_comparison нигде не формализован.
7. **Сшивка многолистовых таблиц**: «шапка наследуется от предыдущего блока цепочки листов» (tables §9) — механизм идентичности цепочки (document_graph порядок страниц + same-table matching) не определён.
8. **Тестирование**: имена гейт-метрик (`dimension_link_rate` / `spacing_zone_link_rate` / `callout_link_rate` / `param_parallel_bind_rate`...) у каждого свои — зафиксировать словарь метрик как контракт конфига profile_rules; корпусный регресс прогонять и на втором объекте (§3.6).

## 5. ЧТО ДОПИСАТЬ В ИТОГОВЫЙ ДОКУМЕНТ (короткий список)

1. **Решение о доме библиотеки**: канон — пакет `graphic_primitives/` в составе integration-design; `dimension_geometry.py`/`construction_anchor_primitives.py`/инлайн в `vector_path_graph.py` — отменить; таблица соответствия P-имён всех 8 дизайнов → канонические функции.
2. **Спека унифицированного dimension-детектора**: 4 режима привязки числа (break/beside/над-интервалом/полочка) + засечки 1.2–6.8 pt @30–60° + двойной штрих + fill-стрелки; per-discipline пресеты в `profile_rules/<disc>.json`; масштаб — per-view.
3. **Один circle-детектор** Ø4–55 с классами (поз-мини/ось/узел/болт-концентрик) и 4 представлениями (full_arc/paired_halves/**l-ring** из АР/polygon).
4. **Владение таблицами**: движок = библиотека, вызывается дисциплинарным профилем; `table_structured` — только fallback; карты findings дверей/отделки/спецификаций — в одном месте (tables-design), АР/КЖ ссылаются.
5. **Единый реестр provenance** (integration §2.2) + новые коды (`ocr_from_vector_glyphs` и др.); словарь primitives quality.py отобразить в реестр.
6. **Стадия cross_block**: контракт, артефакт, без мутации пакетов (снимает конфликт §2.o).
7. **Фазировка**: rotation/crop-клип — за флаг в Фазу 1 (не в «нулевую» Фазу 0), протокол пересъёмки baseline; влитие СС-ветки ДО любых правок роутера.
8. **Политика prepared-reuse в Stage 02** (не гонять тяжёлые детекторы дважды) + капы/стратификация для листов >100К сегментов.
9. **OCR-пилот для glyph-слов** и валидационный корпус второго объекта — как гейты перед фазой раскатки.
10. **Дизайн-карты**: `installation_assembly` (СС, 38 блоков — крупнейший пропуск), решения по `access_point_assembly`/`multidiscipline_niche_layout`; явный список «no-change by design» для зрелых СС-профилей; анти-паттерн «размер со * не в масштабе» и анти-паттерн «N ряд» (кладка) в findings-логике.