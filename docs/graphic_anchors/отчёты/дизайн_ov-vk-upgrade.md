Эмпирика собрана (5 серий проб, 14 PDF). Ключевые числа подтверждены. Составляю дизайн-карту.

# Дизайн-карта апгрейдов геометрий ОВ (154 эталона, 9 профилей) и ВК (168, 11 профилей + water_supply_scheme)

Скрипты проб: `/tmp/claude-1001/-home-coder-projects-PDF-proverka/6f655732-ccf4-4ad5-bf7d-95fbe65d0668/scratchpad/probe_ovvk/probe1_drawings.py … probe4_final.py`. Все правки предлагаются в `backend/app/pipeline/stages/block_grounding/` (hvac_geometry.py, water_geometry.py, water_supply_geometry.py, vector_path_graph.py).

## 0. Сквозные эмпирические факты (фундамент всех карт)

1. **«Нет текст-слоя» = обведённый текст, а не скан.** OV-аксонометрия ДУ `CLLU-QK33-EC9` (text=0): 5150 мелких чёрных fill-путей с `c`-кривыми, h_med=5.04 pt (p10 4.89 / p90 7.14), кластеризуются в **395 слов-боксов**; ВК-план `MDLY-9UXJ-QRP` (text=0): 4204 глифа, h_med=7.09 pt, **510 слов-боксов**. Все 27 «ограниченных» ВК и 6 из 12 ОВ-кандидатов — этот случай.
2. **Стрелка уклона = залитый треугольник с числом на нём.** `4DHK-MVLH-NA4` (аксон. К): 27 fill-треугольников bbox ≈15.8×16.1 pt, **27/27 на дистанции 0.0 pt** от текстов «0,01»/«0,02» — это ГОСТ-обозначение уклона; ориентация треугольника = направление стока.
3. **Подпись параметра параллельна своему сегменту.** Ближайший сегмент к строкам `i=`/`Ø`/`NxN`: **40/40 параллельны** (Δ<12°, дистанция 4.5–6.2 pt) в `4DHK`; 16/17 в `4UCN` (В-аксон.); 76/77 в `76T6` (ОВ вент-аксон.). Направление строки — `line["dir"]` из `get_text("dict")`.
4. **Калибровка осей графика полностью детерминирована.** `4KMQ-9K4P-97R` (насос): Y-ось H=11.223−0.0750·y, max|resid|=**0.000 м** (11 меток); X-ось Q=−2.172+0.0414·x, max|resid|=0.062 м³/ч (10 меток); сетка — единственные в корпусе `dashes="[2] 2"` (10 верт., шаг 24 pt = 1 м³/ч; 10 гор., шаг 12 pt = 0.9 м); кривая = 33 сплошных сегмента → числовые пары (0.36, 7.83), (0.59, 7.67)… ОВ-график `6G36`: кривые — растры 904×745 и 1311×688, но оси/метки векторные + вектор-тики 0.5×12.2 (×30) и 25.7×0.5 (×18) → калибровка растра возможна.
5. **Засечки размеров отделимы от штриховки.** Узел клапана `4TWR`: 850 штрихов 45° длиной 1.2–5 pt; плотные параллельные семьи (шаг проекции ≤1.4 pt) = штриховка; **изолированных (нет соседа <2.5 pt) — 20**, 3 из 6 размерных чисел имеют такую засечку <30 pt. `WYYH`: 2241/95.
6. **Выноски работают без хардкода текстов.** Узел воронки `WYYH`: 18 текст-строк, у **8** диагональный лидер 8–120 pt упирается в bbox («Отвод DN32 45°», «Тройник DN100х50,45°», «Капельная воронка HL21»).
7. **Позиционная окружность.** Смесительный узел `9EEF`: 39 окружностей ⌀22.56–22.8 pt из 4 `c`-item'ов, 14 со словом-позицией внутри («2.13», «2.9», «2.Ж»).
8. **Магистраль в «лесенке» видна.** `6VW4-PCVA-TCN`: 146 стояковых токенов X=162..3110; 8 горизонталей >300 pt в нижней полосе Y 2210–2380; слово «насосной» — кандидат корня.
9. **Продольный профиль имеет сетку и rotation-ловушку.** `7VQA-JP9G-K4D`: rotation=270; 55 вертикалей-ординат (>15 pt), 96 горизонталей, заголовки граф «Уклон», «Основание», «Отметка», «Расстояние»; слова и drawings в ОДНОМ raw-пространстве (raw: 10/23 совпадений X отметка↔ордината ±8 pt; матрицы поодиночке ломают: rot 0/23, derot 5/23) — дератировать надо оба слоя согласованно.
10. **Плотность CAD требует индекса.** `WYYH`: 612 534 l-сегментов в 4032 drawings (med длина 0.0 — флэттенные кривые/штриховка); O(N²) T-junction в `vector_path_graph.py:491` не переживёт. Штрихпунктир нигде (кроме графика) не имеет `dashes` — рисуется микросегментами (медиана длин 0.24–0.34 pt).

## 0б. Новые примитивы общей библиотеки (все — в `vector_path_graph.py`, ярус A/B)

| ID | Примитив | Где менять | Эмпирика |
|---|---|---|---|
| **P1** | `reconstruct_glyph_words()`: fill-пути 0.5–15 pt с `c`-кривыми → кластеры-слова (gap<1.6h, Δy<0.7h) → word-bbox; содержимое — рендер кропа слова → OCR-словарь стал КООРДИНАТНЫМ | новый блок в vector_path_graph.py; вызов из `water_geometry._dispatch` (:459–471) при `no_pdf_text_layer` | факт 1: 395/510 слов-боксов |
| **P2** | `detect_slope_arrows()`: fill-путь из 2–4 `l`, bbox 5–25 pt, текст-строка `0,0\d+` с пересечением bbox → узел `slope` с азимутом (по вершине треугольника) | vector_path_graph.py; потребители — `_build_axonometry` water_geometry.py:224 | факт 2: 27/27 |
| **P3** | `bind_label_to_parallel_segment()`: строка из `get_text("dict")` с `dir` → ближайший сегмент; связь принимается при Δ угла <15° и дистанции < 2.5×высоты строки, иначе `spatial_only` | заменить чисто пространственный `riser_to_parameter` water_geometry.py:236–239 | факт 3: 132/134 |
| **P4** | `calibrate_axes()`: числовые метки вдоль X/Y края поля → две линейные регрессии + residual-гейт (< 1% шкалы); кривая (сплошные сегменты внутри поля) → числовые пары; для растровых кривых — калибровка bbox растра | новый; потребители `_build_chart` hvac_geometry.py:571–594, water_geometry.py:426–444 | факт 4: resid 0.000/0.062 |
| **P5** | `detect_dimension_ticks()`: 45°-штрихи 1.2–5 pt минус плотные параллельные семьи (шаг проекции <2.5 pt) = изолированные засечки; пара засечек на одной прямой + число между ними = размер | vector_path_graph.py + `_physical_nodes` hvac_geometry.py:446 | факт 5: 850→20, 2241→95 |
| **P6** | Генерализация `extract_callout_leaders` (:582–623): убрать хардкод префиксов (:590), принимать любой текст-bbox, допускать полочку (горизонтальный хвост), целевой конец → любой узел/сегмент/штрих-регион | vector_path_graph.py | факт 6: 8/18 без хардкода |
| **P7** | `detect_position_circles()`: замкнутые 3–4 `c`-item'а, w≈h, ⌀15–30 pt, слово внутри → узел `position_callout`; обобщение `detect_terminal_ellipses` (:110–170) за пределы окна 7–10 pt | vector_path_graph.py; потребитель `_build_hydronic` hvac_geometry.py:409–443 | факт 7: 39 окружностей, 14 подписанных |
| **P8** | `profile_grid()`: вертикали-ординаты + длинные горизонтали-графы + текст заголовков граф → таблица «колонка × графа»; ячейки заполняются словами; согласованное дератирование обоих слоёв | water_geometry.py `_build_external` :381–402 | факт 9 |
| **P9** | Пространственный grid-index для T-junction (замена O(N²) :491–506) + извлечение `dashes`/использование `width` в `flatten_line_segments` (:71–107, width уже лежит мёртвым :103) | vector_path_graph.py | факт 10 |
| **P10** | `detect_hatch_regions()`: плотные параллельные семьи 45°-штрихов → полигоны штриховки = зоны материала (для пирогов узлов, как у АР) | vector_path_graph.py | факт 5 |

---

### hvac_floor_plan — планы этажей/кровли/паркинга ОВ (46 блоков, все complete)
- **ЯКОРЯ (текст):** существующие hvac_geometry.py:274–279 достаточны (системы, Ст., оборудование, ø/NxN, помещения).
- **ЯКОРЯ (граф):** vivid-трассы (:153–167) работают; добавить P2-стрелки направления воздуха и P6-выноски к оборудованию.
- **ПРАВИЛА:** без изменений ядра; P3 для подписей `NxN` вдоль воздуховодов (факт 3 подтверждён на аксонометрии — на планах то же начертание).
- **НОВЫЕ ПРИМИТИВЫ:** P3, P6.
- **FINDINGS:** сечение воздуховода NxN по трассе не убывает против потока без перехода (класс: СП 60.13330, аэродинамика); клапан на границе пожарного отсека без `EI` подписи (СП 7.13130).
- **ГЕЙТ:** текущий (:697–698) + `label_parallel_rate ≥ 0.7`.
- **ПРИОРИТЕТ/ЭФФОРТ:** низкий/низкий — профиль уже 46/46 complete.

### heating_axonometry — аксонометрии отопления (15: 8П/7О)
- **ЯКОРЯ (текст):** `_HEAT_PATTERNS` :314–319; добавить обобщённый формат этажа (`L\d+_K\d+` :318 проектно-специфичен — расширить `\d+\s?эт\.|Этаж \d+` + отметки).
- **ЯКОРЯ (граф):** `levels_by_elevation` из water_supply_geometry.py:79–109 (заявлен общим — фактически ОВ его НЕ вызывает; подключить); P3 для Ø-подписей у стояков.
- **ПРАВИЛА:** 1) отметки ±NN,NNN → регрессия Y→отметка (пер-стоячно, как water_supply_geometry.py:209–214); 2) Ø-подпись → параллельный сегмент (P3) → стояк по union-find компоненте (:170–195); 3) 7 «ограниченных» падают на «не извлечены системы» — причина бедный текст → P1-глифы как fallback-словарь марок.
- **НОВЫЕ ПРИМИТИВЫ:** P1, P3 + вынос `levels_by_elevation` в общий модуль.
- **FINDINGS:** немонотонность Ø стояка Т11/Т21 по высоте (класс: СП 60.13330, гидравлика систем отопления); разрыв нумерации этажей (паттерн water_supply_geometry.py:236–245).
- **ГЕЙТ:** текущий (:700–701) + `elevation_regression_quality ≥ 0.7`.
- **ПРИОРИТЕТ/ЭФФОРТ:** средний/средний.

### ventilation_axonometry — аксонометрии вентиляции/ДУ (31: 12П/7О/12К — худший профиль ОВ)
- **ЯКОРЯ (текст):** `_VENT_SYSTEM_RE` :359 полный; проблема не в regex — у 6/12 кандидатов text=0.
- **ЯКОРЯ (граф):** **P1-глифы** (факт 1: 395 слов-боксов на `CLLU-QK33-EC9`) — главный разблокировщик; P2-подобные fill-стрелки направления воздуха (36 шт. 5.4×3.12 pt найдены там же); окружности ⌀22.7 (212 кандидатов) = позиционные/осевые марки; трассы воздуховодов union-find уже есть.
- **ПРАВИЛА:** 1) при `words==0` и `fill_curved_glyphs > 500` → P1: слов-боксы → рендер кропов слов → OCR → координатные марки → штатный `_build_ventilation` (:359–405) без изменений; 2) стрелки → направление потока на трассе; 3) связь с соседним блоком-легендой: `continuation_reference`-механику water_geometry.py:108 перенести в hvac + межблочный ключ = имя системы (В1↔В1 легенды на том же листе через `block_source_router`).
- **НОВЫЕ ПРИМИТИВЫ:** P1 (критичен), P2, P7.
- **FINDINGS:** транзит воздуховода через отсек без нормируемого EI (СП 7.13130); система на схеме без клапана-ДУ при подписи ДУ (СП 7.13130); сечение против расхода (СП 60.13330).
- **ГЕЙТ:** текущий «systems≥2, segments≥30» (:703–704), но с P1-словарём вместо `page.get_text()`; при OCR-происхождении марок — `description_depth=semantic_hierarchy` максимум, `vectograph_level=False` честно.
- **ПРИОРИТЕТ/ЭФФОРТ:** **высокий/средний** — единственный путь поднять 12 кандидатов.

### hydronic_principle — гидравлические принципиальные (7: 6П/1К)
- **ЯКОРЯ (текст):** :409–413; позиции `^\d{1,2}$` уже есть.
- **ЯКОРЯ (граф):** **P7-окружности** ⌀22.6 (факт 7: 39 шт., 14 подписанных) — позиционные выноски; V-стрелки потока штрихами (fill-треугольников 0 — детектор V-пар нужен строгий: ровно 2 штриха 2.5–12 pt с общим концом, угол 20–60°, свободные второй конец; грубая версия даёт 4615 шумовых — нужен фильтр «острие на сегменте»).
- **ПРАВИЛА:** 1) окружность-позиция → лидер (P6) → аппарат; номер позиции = связь со спецификацией (кросс-блочно); 2) `confirmed_pair`-логика (:418–433) остаётся.
- **НОВЫЕ ПРИМИТИВЫ:** P6, P7; V-стрелка — отдельный маленький детектор.
- **FINDINGS:** пропуск в шкале позиций 1..N (комплектность спецификации, ГОСТ Р 21.101 оформление); отсутствие обратного клапана/фильтра в контуре насоса при их наличии в спецификации (класс: СП 60.13330, смесительные узлы) — только как «проверить», по confirmed_pair-цепочке.
- **ГЕЙТ:** текущий (:706–707) + `position_circle_labeled_rate ≥ 0.5`.
- **ПРИОРИТЕТ/ЭФФОРТ:** средний/низкий.

### hvac_installation_detail — монтажные узлы ОВ (28: 19П/9О)
- **ЯКОРЯ (текст):** `_physical_nodes` :446–461 (классы частей, изоляция/огнезащита).
- **ЯКОРЯ (граф):** **P5-засечки** (факт 5: 850→20 изолированных на `4TWR`, 3/6 чисел привязано); **P6-выноски** (без хардкода); **P10-штриховки** = слои пирога (аналог АР); size-regex коллизия `_build_section`:503 решается требованием засечек/выносной линии у числа.
- **ПРАВИЛА:** 1) выноска: текст-bbox → лидер → часть узла (компонента union-find или штрих-регион); 2) размер: пара изолированных засечек на одной прямой + перпендикулярные выносные линии от концов + число у середины (P5) → расстояние привязывается к контуру части через `point_segment_distance`; 3) состав изоляции/проходки: последовательность P10-штрих-регионов поперёк трубы = слои, каждый слой ← выноска с материалом.
- **НОВЫЕ ПРИМИТИВЫ:** P5, P6, P10.
- **FINDINGS:** узел прохода без огнезащитного слоя в выносках при подписи EI (СП 7.13130); толщина изоляции из выноски vs минимум (СП 61.13330 тепловая изоляция); «размерное число без засечек/выносных = не размер» — гасит ложные размеры.
- **ГЕЙТ:** текущий «parts≥3» (:709–710) + `callout_link_rate ≥ 0.4`.
- **ПРИОРИТЕТ/ЭФФОРТ:** **высокий/средний** — 9 «ограниченных» лечатся именно выносками.

### hvac_section_layout — разрезы (13: 8П/3О/2К) и hvac_equipment_drawing — оборудование (3П)
- **ЯКОРЯ:** разрезы — `_views` :464–470 + P5/P6 как у узлов; оборудование — порты/модули :550–553 + P6-лидеры порт→модель.
- **ПРАВИЛА:** контейнеры-виды (:473–479) остаются; text=0 разрезы → P1.
- **НОВЫЕ ПРИМИТИВЫ:** P1, P5, P6.
- **FINDINGS:** отметки на разрезе vs план (кросс-блочная сверка «Проверить по смежным»).
- **ГЕЙТ/ПРИОРИТЕТ:** текущие; низкий/низкий.

### hvac_performance_chart + pump_performance_chart — графики характеристик (10 ОВ + 4 ВК)
- **ЯКОРЯ (текст):** оси :571–573 / water :426–427 достаточны.
- **ЯКОРЯ (граф):** **P4-калибровка** (факт 4 — идеальные регрессии); dashed-сетка `[2] 2` = вспомогательные линии рабочей точки; у ОВ кривые растровые (факт 4: растры 904×745/1311×688 + векторные тики) → калибруется bbox растра.
- **ПРАВИЛА:** 1) метки → 2 регрессии, residual-гейт <1% шкалы; 2) сплошные сегменты внутри поля → полилиния → числовые пары (Q,H); 3) dashed-линии от осей до кривой = заявленная рабочая точка → извлечь (Q₀,H₀); 4) марка насоса/вентилятора из заголовка → **кросс-блочная связь** с маркой на схеме/плане (через реестр блоков листа в `block_source_router`).
- **НОВЫЕ ПРИМИТИВЫ:** P4 (+ чтение `dashes` из P9).
- **FINDINGS:** рабочая точка (Q,H) из таблицы/подписи насоса не лежит на кривой (>5% отклонение) — класс: СП 30.13330/СП 10.13130 (подбор насосов, противопожарные расходы); напор при Q=0 (закрытая задвижка) против прочности сети — «проверить».
- **ГЕЙТ:** `axis_residual_ok И меток ≥ 4 на ось И кривая ≥ 8 точек`; иначе текущий `analytical_geometry`.
- **ПРИОРИТЕТ/ЭФФОРТ:** **высокий/низкий** — самый дешёвый выигрыш: полный пересчёт кривой в числа уже продемонстрирован.

### hvac_site_overview — площадка (1П)
- Оставить как есть (звезда ИТП→корпуса); P6-лидеры при появлении новых блоков. Приоритет: минимальный.

---

### vk_floor_plan — планы ВК (33: 27П/6О)
- **ЯКОРЯ (текст):** ядро :94–124 достаточно; 6 «ограниченных» = text=0 → **P1** (факт 1: `MDLY-9UXJ-QRP` даёт 510 слов-боксов).
- **ЯКОРЯ (граф):** fallback vivid→все цвета уже есть (:203); P2-стрелки уклона на выпусках; P6-выноски к ШПК/воронкам.
- **ПРАВИЛА:** P1-словарь → штатный `_build_plan` (:194–221); стрелка уклона на плане = направление трассы к стояку/выпуску.
- **FINDINGS:** приборы/воронки без трассы (room-binding), пересечение К- и В-трасс в одной компоненте (мнимая связь — уже защищено union-find).
- **ГЕЙТ:** текущий (:560) + при P1 переводить причину «нет текст-слоя» в «текст восстановлен из вектор-глифов (OCR)» с `vectograph_level=False`.
- **ПРИОРИТЕТ/ЭФФОРТ:** высокий/средний (P1 общий с ОВ).

### water_supply_axonometry + fire_water_axonometry — аксонометрии В1/Т3/Т4 и В2/АПТ (6+10)
- **ЯКОРЯ (текст):** :94–124; для fire — ШПК/зоны :169–177.
- **ЯКОРЯ (граф):** **P3** — подпись Ø/материала параллельна сегменту трубы (факт 3: 16/17); стрелки направления; отметки → `levels_by_elevation`.
- **ПРАВИЛА:** 1) `riser_to_parameter` (:236–239) заменить на P3: параметр → параллельный сегмент → стояк через CAD-компоненту сегмента (двухшаговая доказательная привязка вместо «ближайший стояк»); 2) повторные марки — группировка как сейчас (:240–248).
- **НОВЫЕ ПРИМИТИВЫ:** P3.
- **FINDINGS:** Ø по стояку В не растёт вверх (уже есть в лесенке — перенести сюда); зона В2 без ШПК на этаже (СП 10.13130); материал трубы vs огнестойкость шахты (СП 30.13330/СП 10.13130) — «проверить».
- **ГЕЙТ:** текущий + `param_parallel_bind_rate ≥ 0.7`.
- **ПРИОРИТЕТ/ЭФФОРТ:** **высокий/низкий** — P3 маленький, а лечит главную заявленную дыру (пространственная перепривязка параметров).

### sewer_axonometry — аксонометрии К (23: 19П/4О)
- **ЯКОРЯ (текст):** `_SLOPE_RE` :98, `_DIAMETER_RE` :97 есть.
- **ЯКОРЯ (граф):** **P2-стрелка уклона** (факт 2: 27/27 с текстом уклона на нулевой дистанции; вершина треугольника = направление стока) + **P3-параллельность** (факт 3: 40/40).
- **ПРАВИЛА:** 1) уклон-узел = треугольник+число, азимут = направление стока; привязка к трубе — сегмент под стрелкой (psd<6 pt) или параллельный ближайший; 2) Ø — P3 → CAD-компонента → цепочка стояк→отвод→выпуск; 3) на компоненте построить **частичный порядок по направлению стрелок** (новое ребро `flow_direction`).
- **НОВЫЕ ПРИМИТИВЫ:** P2, P3.
- **FINDINGS (детерминированные, сильные):** уклон меньше нормативного минимума для данного Ø (класс: СП 30.13330, самотёчная канализация); **Ø убывает по направлению стока** (сейчас монотонность есть только для «В» в лесенке — water_supply_geometry.py:255 гейтует `startswith("В")`, для К кода нет); стрелки одной компоненты указывают друг на друга (конфликт направлений) → GEOMETRY_CONFLICT.
- **ГЕЙТ:** текущий + `slope_arrows_bound ≥ 1` для перехода в engineering_graph.
- **ПРИОРИТЕТ/ЭФФОРТ:** **максимальный/низкий** — примитив прост, findings прямо нормируемые.

### vk_principle_scheme (20) и fire_control_scheme (2)
- **ЯКОРЯ:** :268–274 / :311–316 достаточны; добавить P7-окружности позиций (водомерные узлы размечаются позициями) и P6-лидеры.
- **ПРАВИЛА:** confirmed_pair-логика (:277–308) остаётся; позиции → спецификация (кросс-блочно).
- **FINDINGS:** водомерный узел без обводной линии при пожарном расходе (СП 30.13330/СП 10.13130) — по наличию параллельной CAD-ветви с задвижкой; шкала позиций без пропусков.
- **ПРИОРИТЕТ/ЭФФОРТ:** средний/низкий.

### vk_installation_detail — узлы ВК (40: 32П/8О) и roof_drain_detail — воронки (10: 8П/2О)
- Правила = карта hvac_installation_detail (P5, P6, P10) плюс:
- **ЯКОРЯ (текст):** `_detail_nodes` :344–363 (fire_seal, well_equipment, construction_layer уже есть — словарь слоёв готов для P10-пирогов).
- **ЯКОРЯ (граф):** факт 6 — на `WYYH` 8/18 строк уже ловятся генерализованным лидером; изолированные засечки 95 при 2241 штрихах (узлы воронок размеряются выносками DN, не линейными размерами — P5 тут вторичен, P6 первичен).
- **ПРАВИЛА (специфика воронки):** цепочка выносок сверху вниз по Y = порядок деталей узла (воронка→отвод→тройник→стояк, у `WYYH` читается прямо: HL21 → Отвод DN32 → Патрубок DN50х32 → Тройник DN100х50); Ур.плиты/отметки — Y-привязка к горизонталям.
- **FINDINGS:** несоответствие DN смежных фитингов в цепочке узла (СП 30.13330); отсутствие гильзы/муфты противопожарной в проходке при пересечении перекрытия (СП 40-107/СП 30.13330) — по словарю fire_seal + P10-область перекрытия; 8+2 «ограниченных» = text=0 → P1.
- **ПРИОРИТЕТ/ЭФФОРТ:** высокий/средний (общая реализация с ОВ-узлами).

### external_network_profile — продольные профили/камеры (11: 9П/2О)
- **ЯКОРЯ (текст):** :383–387 (колодцы, ПК, расстояния) + заголовки граф (факт 9: «Уклон», «Основание», «Отметка», «Расстояние» находятся словами).
- **ЯКОРЯ (граф):** **P8-сетка профиля**: вертикали-ординаты (55 шт. в `7VQA`) + длинные горизонтали-границы граф (>200 pt) = таблица; ⚠ rotation=270: слова и drawings в одном raw-пространстве — согласованное дератирование, НЕ поодиночке (raw 10/23 vs rot 0/23).
- **ПРАВИЛА:** 1) ордината = вертикаль; всё в её X-полосе ±8 pt (отметка земли/лотка, расстояние, колодец) = одна точка профиля; 2) графы = Y-полосы между длинными горизонталями, тип значения = заголовок графы слева; 3) колодец (`ВК-|ДК-`) над своей ординатой; расстояние между ординатами — из графы «Расстояние».
- **НОВЫЕ ПРИМИТИВЫ:** P8 (+ дератификация-хелпер в ярус A).
- **FINDINGS (сильные, чисто арифметические):** i_факт = (отм₁−отм₂)/L vs записанный уклон графы (СП 32.13330 наружная канализация); глубина заложения = отм.земли − отм.лотка < нормативной (СП 32.13330 / СП 31.13330); немонотонность отметок лотка по стоку.
- **ГЕЙТ:** ординат ≥ 4 И граф ≥ 3 И `cell_fill_rate ≥ 0.6`; иначе physical inventory как сейчас.
- **ПРИОРИТЕТ/ЭФФОРТ:** **высокий/средний** — findings арифметические, доказуемые.

### pump_equipment_drawing — чертежи насосов (9: 8П/1О)
- **ЯКОРЯ:** :406–409 + P5-размеры габаритов, P6-лидеры порт→DN.
- **ПРАВИЛА/FINDINGS:** Q/H/⌀ из таблицы блока ↔ марка насоса на схемах (кросс-блочный реестр листа); мощность кВт vs раздел ЭОМ («Проверить по смежным»).
- **ПРИОРИТЕТ/ЭФФОРТ:** средний/низкий.

### water_supply_scheme — стояковые «лесенки» (water_supply_geometry.py, отдельный корпус)
- **ЯКОРЯ (текст):** :34–38 достаточны.
- **ЯКОРЯ (граф):** **длинные горизонтали >300 pt в нижней Y-полосе** = магистрали (факт 8: 8 шт. в полосе 2210–2380); слово «насосной/ввод» = корень.
- **ПРАВИЛА:** 1) корень: узел `main_line` из длинных горизонталей ниже макс. Y этаж-токенов; стояк → магистраль, если X стояка попадает в X-диапазон горизонтали (±45, как :222–227); «насосная/ввод» → ближайшая магистраль; 2) **монотонность Ø для К-систем** — дописать ветку `startswith("К")` (диаметр должен расти вниз) в :246–257, убрав мёртвый код :252–253; 3) **rotation**: прогнать words через `_point`-преобразование как в hvac (:88–99) — сейчас сырые `get_text("words")`; 4) добавить `field_state`/bbox_norm узлам (унификация с конвертом schema_version=1).
- **НОВЫЕ ПРИМИТИВЫ:** детектор `main_line` (мини-P12, локальный).
- **FINDINGS:** стояк без связи с магистралью = «висячий» (СП 30.13330) — только при построенной магистрали; существующие «разрыв стояка»/«немонотонность В» остаются.
- **ГЕЙТ:** текущий каскад + `mains_detected ≥ 1` для повышения depth.
- **ПРИОРИТЕТ/ЭФФОРТ:** средний/низкий — точечные правки готового модуля.

---

## Сводный приоритет (ценность × усилие)

1. **P2+P3 → sewer/water аксонометрии** (уклоны+направление стока+параллельная привязка параметров): минимальный код, нормируемые findings, 27/27 и 132/134 эмпирика.
2. **P4 → графики ОВ+ВК** (14 блоков): нулевые остатки регрессии, кривая в числах, кросс-блочная сверка рабочих точек насосов/вентиляторов.
3. **P1-глифы** (27 ВК «ограниченных» + 6 ОВ text=0 кандидатов + разрезы): один примитив поднимает ~35 блоков с inventory до semantic; OCR только по словным кропам, координаты — из вектора.
4. **P8-профили** (11 блоков): арифметические findings уклон/заложение.
5. **P5+P6+P10 → узлы** (28 ОВ + 40 + 10 ВК): выноски без хардкода, изолированные засечки, пироги слоёв.
6. **Лесенка**: магистраль+корень, монотонность К, rotation.
7. **P9-инфраструктура** (grid-index, dashes/width): обязательный фундамент под 2, 4, 5 (612k сегментов у `WYYH`).

Ограничения зафиксированы: dashes в CAD-экспорте почти не живут (штрихпунктир = микросегменты — детектор осевых по dashes строить нельзя, только по коллинеарным цепочкам коротких сегментов); стрелки потока в гидравлике — open-V штрихами, нужен строгий детектор (грубый даёт 4615 ложных); P1-текст всегда помечать `evidence_state="ocr_from_vector_glyphs"` и не давать ему `vectograph_level=True`.